# 01 — Скелет приложения

**What to build:** Локально одной командой поднимается Gaintt: React собирается,
FastAPI отдаёт его статикой и отвечает на проверку живости, SQLite создаётся при
старте. Содержимого ещё нет — есть работающий сквозной путь от репозитория до
браузера, на который дальше наращивается всё остальное.

**Blocked by:** None — can start immediately.

**Status:** implemented-with-workspace-blocker

- [ ] Репозиторий инициализирован, `CONTEXT.md`, `docs/` и спека в истории
- [x] React-приложение собирается одной командой
- [x] FastAPI отдаёт собранный фронтенд и эндпоинт проверки живости
- [x] SQLite в WAL, схема создаётся при старте, один процесс
- [x] Секреты (ключ OpenRouter) читаются из окружения, в репозитории их нет
- [x] README содержит инструкцию локального запуска

Примечание: инициализация `.git` была попытана, но текущая среда запрещает запись
в `.git`; исходные `CONTEXT.md`, `docs/` и спека сохранены.
